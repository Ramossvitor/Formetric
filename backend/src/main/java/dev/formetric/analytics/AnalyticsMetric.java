package dev.formetric.analytics;

/** Metrics supported by the bounded daily time-series endpoint. */
public enum AnalyticsMetric {
    CALORIES,
    PROTEIN,
    CARBOHYDRATE,
    FAT,
    FIBER,
    WATER,
    ENERGY_BALANCE,
    WEIGHT
}
