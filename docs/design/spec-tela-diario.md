MESMO gutter de 20px, mesmo `padding-bottom: calc(76px + safe + 40px)`, mesmo header sticky.

[80-160] PAGE-HEADING. `<p class="eyebrow">Registro diário</p>` 12px; `<h1>` MANTIDO com as strings atuais 'Hoje' | 'Diário' em 28px fixo (e2e formetric.spec.ts:115 e :118 dependem de heading name 'Diário'); `<p class="heading-copy date-copy">{displayDate(date, locale)}</p>` mantido em 15px --text-muted (era 0.88rem, mas a classe é global).
  A `.date-navigation` de hoje — dois `.icon-button` de 34px, um `<input type=date>` de 40px e o `.text-button` 'Ir para hoje' em flex-wrap — é substituída por um botão-calendário de 44x44 (radius 14, border 1px --border, --surface, `<Icon name="calendar" size={18} />`, `aria-label="Selecionar data"`) com o `<input type="date" id="diary-date">` invisível (opacity 0, inset 0) por cima, preservando o `<span class="visually-hidden">Selecionar data</span>` atual. Fica alinhado à direita do heading.

[168-232] FAIXA DE 7 DIAS, 64px de altura (56px de pílula + 8px de folga). NÃO é sticky (uma única camada pegajosa na tela: o header).
  `display:flex; gap:8px; overflow-x:auto; scroll-snap-type:x mandatory; scroll-padding-inline:20px; margin-inline:-20px; padding-inline:20px; scrollbar-width:none` (+ `::-webkit-scrollbar{display:none}`).
  Janela: `[date-5 … date+1]` clampada para não ultrapassar `today`; quando `date === today`, `[today-6 … today]`.
  Cada pílula: `<button>` 44x56, radius 16, scroll-snap-align center, grid de 2 linhas — dia da semana 12px/700 uppercase --text-soft, número 17px/700 tabular. `aria-pressed` no selecionado, `aria-label` com a data por extenso.
    - selecionada: background --primary-strong, texto #fff (dark: --accent, texto #102017)
    - hoje, não selecionado: border 1px --accent-strong
    - futura: `disabled`, opacity .4
  SEM ponto de 'dia com registro': não existe endpoint de dias-com-registro e derivar de /analytics/series seria uma query mensal extra para desenhar 4px.
  O `.text-button` 'Ir para hoje' (só quando `date !== today`) fica numa linha própria de 40px à direita, 13px/650 --primary, alvo de 44px — deixa de disputar largura com as setas.

[244-284] `.diary-status-row` vira UMA linha de 40px, radius 12, padding 0 14px, flex gap 10px: ponto de 8px + `<span class="diary-status">` com statusLabel em 15px/650 + o segundo `<span>` em 15px --text-muted. AMBAS as strings atuais preservadas: 'Fechado em {…}' (DiaryPage.test.tsx:122) e 'Alterações são salvas imediatamente'. Fundo: aberto transparente, fechado --blue-soft.

[quando !open] `.closed-notice` MANTIDO com o texto atual — 'Histórico confirmado' é asserção viva (DiaryPage.test.tsx:235, e2e:168/:173). Restyle: caixa de 2 linhas, radius 12, --blue-soft, padding 12px 14px, `<strong>` 15px/700 --blue e `<span>` 13px/1.5 --text-muted.

[DiarySummary] FICA NO TOPO, antes das refeições. Rejeito explicitamente movê-lo para um sheet no fim. Reforma 100% de pele, sem tocar na ordem nem nas strings do TSX (exceto o disclosure):
  - `.diary-summary` radius 24 (era 20), border 1px --border, --shadow.
  - `.diary-calories` mantém o `linear-gradient(135deg, primary-soft 55%, surface)` — identidade aprovada — e ganha padding 24px 20px. `<h2 id="diary-summary-title">` em 40px/700 tabular -0.03em, com `<small>kcal</small>` em 17px/650 (o e2e:161 espera `heading name '84 kcal'`, e a string composta é idêntica). Sem registro nutricional: 'Não informado' em 24px/700 --text-soft — nunca '0 kcal'.
  - `<p>` da meta nominal: 13px --text-soft (era 0.69rem). Strings intactas.
  - `.energy-pill`: min-height 56px, radius 16, padding 0 16px, grid de 3 linhas. `<span>` 12px/700 uppercase; `<strong>` 20px/700 tabular --text; `<small>` (TDEE) 13px. Quando `energy == null`, o `<strong>` cai para 13px/1.4 e quebra em até 2 linhas com a mensagem real ('Cadastre um TDEE para esta data' | 'Registre alimentos ou confirme o jejum para calcular o saldo') — DiarySummary.test.tsx:165 e :217 dependem dessa string. Cores atuais (deficit/surplus/unavailable) mantidas.
  - `.diary-macro-grid`: as SEIS células ficam (sódio E água), porque DiarySummary.test.tsx:162-164 navega o DOM por `.diary-macro-grid` e espera '0,5 L' lá dentro; tirar água ou sódio é risco gratuito. Muda só a pele: `gap: 0; background: transparent; border-top: 1px solid var(--border)`, cada `> div` com `padding: 14px 16px`, `border-bottom: 1px solid var(--hairline)` e `border-right` nas células ímpares. `dt` 13px/650 --text-muted (era 0.62rem); `dd` 20px/700 tabular (era 0.8rem). A grade de linhas de 1px que fazia a coisa parecer planilha desaparece sem que nenhuma célula saia.
  - DISCLOSURE `.goal-state-list`: precedido por um `<button aria-expanded>` de 48px, largura total, border-top 1px --border, texto 'Ver classificação das metas' 15px/700 --primary à esquerda e chevron 18px --text-soft à direita que gira 90°. O wrapper usa `display:grid; grid-template-rows: 0fr; transition: grid-template-rows 240ms` -> `1fr` quando aberto, com `overflow:hidden` e `opacity 0->1` em 160ms delay 80ms. NUNCA display:none nem hidden — o conteúdo permanece no DOM e os 5 casos de DiarySummary.test.tsx (getByText de '2.137 kcal / meta ≥ 2.400 e ≤ 2.600 kcal', getByRole('group', {name: /Fibras: classificação parcial/}), etc.) continuam verdes sem uma linha reescrita.
  - `.goal-state-list` vira UMA coluna (era 2), gap 0, padding 0. Cada `.goal-state` vira linha de 64px: `border-left: 3px solid` na cor do tom (POSITIVE --accent-strong, WARNING --orange, NEUTRAL --blue, sem tom --border-strong), padding 12px 16px, `border-bottom: 1px solid var(--hairline)`, grid [1fr | auto]. `<span>` nome 15px/600 (col 1, row 1); `<strong>` 15px/700 tabular alinhado à direita (col 2, row 1); `<small>` 13px --text-soft (col 1/-1, row 2). Era tudo 0.59rem. Reforma PURAMENTE CSS: a ordem dos elementos no TSX não muda.
  - As DUAS `.summary-footnote` ficam (a segunda é asserção em DiarySummary.test.tsx:119), em 13px/1.5 --text-soft, padding 14px 16px.

[REFEIÇÕES] `.section-title-row`: o eyebrow 'Alimentação' sai; `<h2 id="meals-title">Refeições</h2>` vira 13px/750 uppercase 0.06em --text-soft; o `.compact-button` '+ Refeição' (string obrigatória, DiaryPage.test.tsx:242 e :330) vai a min-height 44px, 14px/700, radius 999.
  `.meal-card`: radius 20 (era 18), border 1px --border, --shadow, gap 12px entre cards.
  CABEÇALHO, min-height 64px, padding 16px, `grid-template-columns: minmax(0,1fr) auto 44px`, gap 12px, align-items center:
    - col 1: `<span>` hora 13px/700 tabular --text-soft ('12:30' ou 'Refeição N'); `<h3>` 17px/650 margin-top 2px, ellipsis.
    - col 2: `<strong>` '612 kcal' 17px/700 tabular; `<small>` '42 g proteína' 13px --text-soft.
    - col 3: UM `<button class="icon-button" aria-label={'Ações de '+meal.name}>` de 44x44 com um SVG novo `more` (três pontos verticais) no Icon.tsx.
    - `.meal-actions { grid-column: 1 / -1 }` É ELIMINADA. Hoje os três `.icon-button` de 34px com gap 4px (App.css:2330) ocupam uma SEGUNDA LINHA INTEIRA por refeição; num dia de 4 refeições isso é ~200px de cromo e três alvos pequenos com o destrutivo colado no benigno.
  SHEET DE AÇÕES DA REFEIÇÃO: reusa `DiaryDialog` com `title={meal.name}`. Três linhas de 56px, largura total, 17px/650, hairline entre elas, com os aria-labels ATUAIS preservados palavra por palavra: `Editar {meal.name}`, `Duplicar {meal.name}`, `Excluir {meal.name}` (a última em --danger). Excluir confirma IN PLACE: a linha se transforma em duas de 56px, 'Confirmar exclusão' (--danger sólido, texto #fff) e 'Cancelar'. Estados pending: opacity .5 e texto 'Duplicando…'. Nenhum teste atual clica nesses três botões, então o custo em teste é zero.
    TRADE-OFF ASSUMIDO: duplicar refeição passa de 1 para 2 toques. Se o dono reclamar (ele come repetido), a saída barata é promover só 'Duplicar' de volta ao cabeçalho como um segundo botão de 44px — o cabeçalho tem folga depois que a segunda linha morreu.
  ITENS, `<li>` min-height 68px, padding 14px 16px, `grid-template-columns: minmax(0,1fr) auto 44px`, gap 4px 12px, `border-bottom: 1px solid var(--hairline)`. DUAS colunas de conteúdo, não quatro:
    - `.item-primary` (col 1, row 1): `<strong>` nome 15px/650 ellipsis (era 0.77rem); `<span>` 13px --text-soft com a string ATUAL '150 g · alimento · v. preservada' — 'v. preservada' FICA, porque DiaryPage.test.tsx:184 a asserta e cortá-la não devolve altura nenhuma.
    - `.item-nutrition` (col 2, row 1, text-align right): `<strong>` '248 kcal' 15px/700 tabular; `<span>` 'P 24 · C 3 · G 9' 13px --text-soft. P/C/G CONTINUA VISÍVEL NA LINHA — rejeito escondê-lo em sheet.
    - `.item-quality` (col 1/-1, row 2, flex gap 8px): renderizado APENAS quando `dataQuality !== 'EXACT'`. `.quality-chip` min-height 24px, padding 0 10px, 12px/700 (era 22px/0.56rem), cores atuais; `± 40 kcal` em 13px --text-soft. Ausência de chip passa a significar exato.
    - col 3, row 1: UM `<button aria-label={'Ações de '+item.name}>` de 44x44 com o ícone `more`, abrindo um sheet com duas linhas de 56px cujos aria-labels são os ATUAIS — `Editar {item.name}` e `Excluir {item.name}` (esta com confirmação in place). As `.item-actions` de dois `.icon-button` de 34px com `gap: 3px` somem da lista; é isso que devolve a largura da linha.
  `.meal-empty`: 56px, 15px --text-soft, string atual.
  `.add-item-button`: min-height 52px (era 45), 15px/700 (era 0.7rem), com `<Icon name="plus" size={18} />` à esquerda e gap 8px. String '+ Adicionar alimento ou receita' MANTIDA (DiaryPage.test.tsx:175 e e2e:150).

[ÁGUA] `.water-section` padding 20px, radius 20. `<h2 id="water-title">Água · {n} L</h2>` — string obrigatória (e2e:164 espera /Água · 0,25 L/) — em 20px/700 com o número tabular.
  `.water-buttons`: UMA fila de 4 (`grid-template-columns: repeat(4, minmax(0,1fr))`, gap 8px) em vez da grade 2x2. Cada botão min-height 56px (era 42), radius 16, 15px/700, letter-spacing -0.01em, white-space nowrap, padding 0 6px, cores atuais (--blue-soft/--blue com border color-mix). Strings '+250 ml', '+500 ml', '+750 ml', '+1 L' MANTIDAS (DiaryPage.test.tsx:207, e2e:163). Em 390px sobram ~71px por botão; abaixo de 360px de viewport, fallback para 2x2 via `@media (max-width: 359px)`.
  `.water-history` FICA VISÍVEL (colapsá-la quebraria DiaryPage.test.tsx:123, que faz getByText('07:30')). Linhas de 56px (era 43), `grid [1fr auto 44px]`, `<time>` 13px tabular --text-muted, `<strong>` 15px/700 tabular, e o botão de excluir a 44x44 com o aria-label ATUAL 'Excluir água de {n} ml' (DiaryPage.test.tsx:211). NADA de swipe: o botão visível é o único caminho, e ele é acessível por teclado e leitor de tela.
  Sem registros: `.inline-hint` 'Nenhum registro de água.' em 15px --text-soft.

[AÇÕES DO DIA] `.diary-day-actions` card, padding 20px, radius 20, margin-top 32px. `<strong>` 17px/650 e `<span>` 15px/1.5 --text-muted (strings atuais). Os botões deixam de dividir a largura com `flex: 1` (que dá 171px cada num aparelho de 390px, com a ação terminal do dia do tamanho de um utilitário) e passam a ser EMPILHADOS, largura total, min-height 52px, na ordem: 'Fechar dia' (submit-button, primeiro) e 'Copiar registros' (secondary). Fechado: 'Reabrir dia' único, secondary, 52px. TODAS as strings mantidas (DiaryPage.test.tsx:231/:241/:259/:281/:289, e2e:166/:169). 'Fechar dia' NÃO é promovido a botão flutuante — induz fechamento precoce.

[DIA SEM REGISTRO] `.diary-empty`: o glifo tipográfico '＋' vira `<Icon name="plus" size={24} />` num círculo de 56px --primary-soft. `<h2>Nenhum registro neste dia</h2>` (string obrigatória, DiaryPage.test.tsx:141) em 22px/700; `<p>` 15px/1.55. As `.empty-actions` deixam de ser flex-wrap com botões de 43px em duas linhas irregulares e viram uma coluna: três botões de largura total e 52px, na ordem 'Adicionar refeição' (submit; e2e:144 depende), '+250 ml de água', 'Copiar outro dia'. Strings mantidas.

[DIÁLOGOS] `.dialog-backdrop` e `.diary-dialog` ganham o movimento descrito no bloco de sistema e um handle de 40x4px --border-strong, radius 2, centralizado 8px abaixo do topo do sheet. `.quick-action-grid` (o cadastro rápido do FAB) tem suas linhas elevadas a 64px com `<strong>` 15px/650 e `<span>` 13px. Sem arrasto-para-fechar (Pointer Events na mão foi rejeitado); fechar continua sendo o botão × de 44px e Escape.

CONTAGEM DE TOQUES (regressão zero nas tarefas frequentes): adicionar item = 1 toque (inalterado); +250 ml no Diário = 1 (inalterado); +250 ml a partir da Home = 1 (era 3+navegação); trocar de dia = 1 na faixa (era 1 numa seta de 34px ou 2 no picker); editar item = 2 (era 1); excluir item = 3 com confirmação (era 1 num alvo de 34px colado noutro); duplicar refeição = 2 (era 1).